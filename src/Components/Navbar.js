import React, { useState, useEffect, useRef } from 'react';
import { Links } from '../data';
import '../main.css';

const Navbar = () => {
  return (
    <section>
      {' '}
      <h1>Hello world</h1>
    </section>
  );
};

export default Navbar;
